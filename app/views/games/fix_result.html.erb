<h1>Spiel bearbeiten</h1>

<%= form_for(@game, :url => save_result_game_path(@game)) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="fixture">
  	<%= @game.hometeam %> : <%= @game.awayteam %>

   <div class="field">
    <%= f.label :homescore %><br />
    <%= f.number_field :homescore %>
  </div>
  <div class="field">
    <%= f.label :awayscore %><br />
    <%= f.number_field :awayscore %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to "Ergebnisse eintragen", :controller => "games", :action => "result_index"%>