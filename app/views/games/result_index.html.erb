<h3>Ergebnisse eintragen</h3>

<table class="table table-striped table-condensed">
  <tr>
    <th>Kick Off</th>
    <th>Home Team</th>
    <th>Away Team</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @games.each do |game| %>
  <tr>
    <td><%= game.kickoff.strftime("%d.%m.%y %H:%M") %></td>
    <td><%= game.hometeam %></td>
    <td><%= game.awayteam %></td>
    <td><%= link_to "Ergebnis eintragen", fix_result_game_path(game) %></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'Zurück zur Admin Übersicht', admin_path %>