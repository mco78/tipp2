<div id="header">
  <div id="head_right">
    <div id="session_info">
      <% if user_signed_in? %>
          Signed in as <%= current_user.email %>. Not you?
          <%= link_to "abmelden", destroy_user_session_path, :method => :delete %>
      <% else %>
          <%= link_to "registrieren", new_user_registration_path %> or
          <%= link_to "anmelden", new_user_session_path %>
      <% end %>

      <% if admin_signed_in? %>
          <br> Signed in as admin <%= current_admin.email %>. Not you?
          <%= link_to "abmelden", destroy_admin_session_path, :method => :delete %>
      <% end %>
    </div>
    </br>
    <div id="timenow">
      <%= Time.now.strftime("%d.%m.%y %H:%M") %> 
    </div>
  </div>

  <ul id="nav">
    <li id="li_home"><%= link_to "Home", root_path %></li>
    <li id="li_abgabe"><%= link_to "Tippabgabe", abgabe_path %></li>
    <li id="li_uebersicht"><%= link_to "Tippübersicht", uebersicht_path %></li>
  </ul>
    
</div>