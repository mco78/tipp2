<div class="row">
	<div class="span8" id="welcome">
		<div class="hero-unit">
			<h1>tipp2 - Das Fussball Tippspiel</h1>
			<p>tipp2 ist ein klassisches Fussball-Tippspiel. Melde Dich an, tippe auf Spiele, sammel Punkte und gewinne gegen andere Spieler.</p>
			
			<% if user_signed_in? %>
				<p>
					<%= link_to "zur Tippabgabe", abgabe_path, :class => "btn btn-primary btn-large" %>
				</p>
			<% else %>
				<p>
					<%= link_to "Jetzt registrieren", new_user_registration_path, :class => "btn btn-primary btn-large" %>
				</p>
					<%= link_to "Schon angemeldet? Log in!",  user_session_path, :class => "zusatzlink" %>
			<% end -%>
		</div>
	</div>

	<div class="span4">
		<h2>Neuigkeiten</h2>
		<p></p>
		<% @posts.each do |post| %>
			<%= post.created_at.strftime("%d.%m.%y %H:%M")%>
			<h3><%= post.headline %></h3>
			<p><%= truncate(post.content, :length => 120, :separator => ' ') %>
				<% if post.content.length > 120 %>
					<%= link_to "mehr", post %>
				<% end %>
			</p>
		<% end %>
		<h3><%= link_to "Alle News", news_path %></h3>
	</div>
</div>


