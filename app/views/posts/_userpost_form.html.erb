<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id, :value => @user.id %>
  <%= f.hidden_field :category, :value => "com" + @community.id.to_s %>
  <div class="field">
    <%= f.label "Überschrift:" %><br />
    <%= f.text_field :headline %>
  </div>
  <div class="field">
    <%= f.label "Inhalt:" %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>