<p id="notice"><%= notice %></p>

<h2><%= @post.headline %></h2>

<p>Erstellt: <%= @post.created_at.strftime("%d.%m.%y %H:%M") %> | Geändert: <%= @post.updated_at.strftime("%d.%m.%y %H:%M") %>
</p>

<p><%= @post.content %></p>

<% if admin_signed_in? %>
    <%= link_to 'Edit', edit_post_path(@post) %> |
	<%= link_to 'Back', posts_path %>
<% else %>
	<b>Hier muss noch ein Link für Nicht-Admins hin...</b>
<% end %>


